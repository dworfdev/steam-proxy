{
  "name": "steam-inventory-proxy",
  "version": "1.0.0",
  "description": "Small proxy service that fetches Steam inventories using Node's fetch (which passes Steam's bot detection, unlike the Java backend's TLS fingerprint).",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "express": "^4.19.2",
    "https-proxy-agent": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
